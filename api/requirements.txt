# Python packages the API server needs.
# Render (see ../SETUP.md -> "Running the API online (Render)") installs
# these automatically before every deploy -- you don't need to install
# anything yourself unless you're running the API on your own computer to
# test it first.

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30

# Same database driver the crawlers already use, kept unpinned-to-an-exact-
# version here deliberately -- see ../requirements.txt for the pinned
# version if you ever want the two to match exactly.
psycopg2-binary>=2.9
